export type PsiResult = {
  url: string;
  performance: number;
  fcp: number;
  lcp: number;
  tbt: number;
  cls: number;
  speedIndex: number;
  timeToInteractive: number;
  serverResponseTime: number;
  requests: number;
}
